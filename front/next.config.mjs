// next.config.mjs — jeromemarichez2026
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone', // image Docker runtime minimale (voir Dockerfile)
}

export default nextConfig
